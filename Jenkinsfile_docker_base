pipeline {
    agent any
    stages {
        stage('build-docker-base') {
            steps {
                sh "docker build -t haris99/ubuntu_ruby_base:latest . -f Dockerfile_base"
            }
        }
    }
}